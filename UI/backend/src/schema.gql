type Book {
    id: ID!
    title: String!
    shelfNo: String!
}

type Query {
    books: [Book!]!
}

type Mutation {
    createBook(title: String!, shelfNo: String!): Book!
    deleteBook(id: ID!): Book!
}